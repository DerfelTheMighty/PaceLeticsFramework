@page "/Athletes/trainingplanpage"
@using AthleteDataAccessLibrary.Contracts
@using PaceLetics.AthleteModule.CodeBase.Models
@using PaceLetics.CoreModule.Infrastructure.Constants
@using PaceLetics.CoreModule.Infrastructure.Models
@using PaceLetics.TrainingModule.CodeBase.Running.Models
@using PaceLetics.TrainingModule.Components.Running
@using PaceLetics.TrainingPlanModule.CodeBase.Models
@using PaceLetics.Web.Services
@using Microsoft.JSInterop
@inject IJSRuntime JS
@inject AuthenticationStateProvider GetAuthenticationStateAsync
@inject IAthleteData AthleteData
@inject IDialogService dialogService
@inject IStringLocalizer<TrainingPlanPage> L
@inject ITrainingPlanService TrainingPlanService
@inject IAchievementService AchievementService
@inject NavigationManager Navigation
@inject ISnackbar Snackbar


<MudContainer Class="@ContainerClass" MaxWidth="MaxWidth.Small">

@if (Embedded)
{
    <MudStack Spacing="1">
        <MudText Typo="Typo.h6">@L["Title"]</MudText>
        <MudText Typo="Typo.body2" Color="Color.Secondary">@L["Subtitle"]</MudText>
    </MudStack>
}
else
{
    <PageHeader Title="@L["Title"]"
                Subtitle="@L["Subtitle"]" />
}

<MudDivider Class="my-4" />

<MudStack Spacing="2" Class="mb-4">
    @if (_plans == null || !_plans.Any())
    {
        <MudAlert Severity="Severity.Info" Variant="Variant.Outlined">
            <MudStack Row="true"
                      AlignItems="AlignItems.Center"
                      Justify="Justify.SpaceBetween"
                      Style="width:100%; gap:12px;">
                <MudText Typo="Typo.body2">@L["NoPlans"]</MudText>
                <MudTooltip Text="@L["CourseSelectionLink"]">
                    <MudIconButton Href="/Athletes/courses"
                                   Icon="@Icons.Material.Filled.School"
                                   Color="Color.Primary"
                                   aria-label="@L["CourseSelectionLink"]" />
                </MudTooltip>
            </MudStack>
        </MudAlert>
    }
    else
    {
        <div class="pl-training-plan-grid">
            @foreach (var plan in _plans)
            {
                var isSelected = plan.Id == _selectedPlanId;
                var isSubscribed = IsPlanSubscribed(plan.Id);

                <MudPaper Elevation="@(isSelected ? 4 : 1)"
                          Class="@GetPlanCardClass(isSelected)"
                          role="button"
                          tabindex="0"
                          aria-pressed="@isSelected"
                          aria-label="@string.Format(L["SelectPlanNamed"], plan.Name)"
                          @onkeydown="@(args => HandlePlanCardKeyDown(args, plan.Id))"
                          @onclick="@(() => SelectPlanAsync(plan.Id))">
                    <MudStack Spacing="2" Style="height:100%;">
                        <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Style="gap:8px;">
                            <MudIcon Icon="@Icons.Material.Filled.EventNote" Color="Color.Primary" Size="Size.Small" />
                            @if (isSubscribed)
                            {
                                <MudChip T="string" Size="Size.Small" Color="Color.Success" Variant="Variant.Filled">@L["Subscribed"]</MudChip>
                            }
                        </MudStack>

                        <MudText Typo="Typo.subtitle2" Style="line-height:1.25;">@plan.Name</MudText>
                        <MudText Typo="Typo.caption" Color="Color.Secondary">@FormatPlanSummary(plan)</MudText>
                        <MudText Typo="Typo.caption" Color="Color.Secondary">@FormatPlanRange(plan)</MudText>

                        <MudSpacer />

                        <MudButton Size="Size.Small"
                                   Variant="@(isSubscribed ? Variant.Outlined : Variant.Filled)"
                                   Color="@(isSubscribed ? Color.Default : Color.Primary)"
                                   StartIcon="@(isSubscribed ? Icons.Material.Filled.NotificationsOff : Icons.Material.Filled.NotificationsActive)"
                                   OnClick="@(() => TogglePlanSubscriptionAsync(plan.Id))">
                            @(isSubscribed ? L["Unsubscribe"] : L["Subscribe"])
                        </MudButton>
                    </MudStack>
                </MudPaper>
            }
        </div>
    }
</MudStack>

@if (Plan is not null)
{
    <MudPaper Elevation="0" Class="pa-4 pl-course-detail">
        <MudStack Spacing="2">
            <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Style="gap:12px;">
                <MudText Typo="Typo.subtitle2">@Plan.Name</MudText>
                <MudText Typo="Typo.caption" Color="Color.Secondary">@FormatPlanSummary(Plan)</MudText>
            </MudStack>

            <MudStack Spacing="1">
                @foreach (var session in Plan.Sessions)
                {
                    var isNext = session.Id == _nextSessionId;

                    <div id="@(isNext ? "next-session" : null)"
                         class="@GetSessionRowShellClass(isNext)">
                        <button type="button"
                                class="@GetSessionRowClass(isNext)"
                                @onclick="@(() => OpenDetails(session.Id))">
                            <span class="pl-training-session-date">
                                <span>@session.Date.ToString("dd.MM.")</span>
                                @if (session.Appointment.StartsAt is not null)
                                {
                                    <small>@session.Appointment.StartsAt.Value.ToString("HH:mm")</small>
                                }
                            </span>
                            <span class="pl-training-session-copy">
                                <span class="pl-training-session-title">@session.Name</span>
                                <span class="pl-training-session-meta">@GetSessionTimelineDetail(session)</span>
                            </span>
                            @if (isNext)
                            {
                                <MudChip T="string" Size="Size.Small" Color="Color.Primary" Variant="Variant.Outlined">@L["NextSession"]</MudChip>
                            }
                            <MudIcon Icon="@Icons.Material.Filled.ChevronRight" Size="Size.Small" />
                        </button>

                        <MudTooltip Text="@GetCompletionTooltip(session)">
                            <MudCheckBox T="bool"
                                         Color="Color.Success"
                                         Value="@IsSessionCompleted(session)"
                                         ValueChanged="@((bool value) => ToggleSessionCompletionAsync(session, value))"
                                         Disabled="@(_isSavingCompletion || Plan is null)"
                                         aria-label="@GetCompletionTooltip(session)" />
                        </MudTooltip>
                    </div>
                }
            </MudStack>
        </MudStack>
    </MudPaper>
}
@if (_detailsOpen)
{
    <div class="pl-overlay"
         @onclick="CloseDetails">
    </div>
}

<div style="
     position:fixed;
     left:0; right:0; bottom:0;
     z-index:1301;
     transform: translateY(@(_detailsOpen ? "0" : "105%"));
     transition: transform 180ms ease;
     pointer-events:@(_detailsOpen ? "auto" : "none");
"
     role="dialog"
     aria-modal="true"
     aria-hidden="@(!_detailsOpen)"
     aria-labelledby="training-session-details-title"
     @onkeydown="HandleDetailsKeyDown">
    <MudFocusTrap Disabled="@(!_detailsOpen)">
    <MudPaper Elevation="12"
              Class="pl-bottom-sheet">

        <div style="padding:12px 16px;">
            <div class="pl-bottom-sheet-handle"></div>

            <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween" Style="width:100%;">
                <MudText Typo="Typo.h6" HtmlTag="h2" id="training-session-details-title">@(_selectedTrainingSession?.Name ?? _resolved?.Name ?? L["Session"])</MudText>
                <MudIconButton Icon="@Icons.Material.Filled.Close" OnClick="CloseDetails" aria-label="@L["Close"]" />
            </MudStack>

            @if (_selectedTrainingSession is not null)
            {
                <MudText Typo="Typo.caption" Color="Color.Secondary">
                    @GetSessionDetailSummary(_selectedTrainingSession)
                </MudText>
            }
        </div>

        <MudDivider />

        <div style="height: calc(100% - 78px); overflow:auto; padding:12px 16px;">
            @if (_selectedTrainingSession is null)
            {
                <MudText Color="Color.Secondary">@L["NoDetails"]</MudText>
            }
            else
            {
                <MudStack Spacing="2">
                    @if (_selectedTrainingSession.HasAppointment)
                    {
                        <MudPaper Elevation="0" Class="pa-3 pl-bottom-sheet-item">
                            <MudStack Spacing="1">
                                <MudText Typo="Typo.subtitle2">@L["Appointment"]</MudText>
                                <MudText Typo="Typo.caption" Color="Color.Secondary">
                                    @FormatAppointment(_selectedTrainingSession.Appointment, _selectedTrainingSession.Date)
                                </MudText>
                                @if (!string.IsNullOrWhiteSpace(_selectedTrainingSession.Appointment.Location))
                                {
                                    <MudText Typo="Typo.caption" Color="Color.Secondary">
                                        @($"{L["Location"]}: {_selectedTrainingSession.Appointment.Location}")
                                    </MudText>
                                }
                                @if (!string.IsNullOrWhiteSpace(_selectedTrainingSession.Appointment.Notes))
                                {
                                    <MudText Typo="Typo.caption" Color="Color.Secondary">
                                        @($"{L["Notes"]}: {_selectedTrainingSession.Appointment.Notes}")
                                    </MudText>
                                }
                            </MudStack>
                        </MudPaper>
                    }

                    @if (_selectedTrainingSession.HasTrainingEffect)
                    {
                        <MudPaper Elevation="0" Class="pa-3 pl-bottom-sheet-item">
                            <MudStack Spacing="1">
                                <MudText Typo="Typo.subtitle2">@L["TrainingEffect"]</MudText>
                                @foreach (var line in GetTrainingEffectLines(_selectedTrainingSession.TrainingEffect))
                                {
                                    <MudText Typo="Typo.caption" Color="Color.Secondary">@line</MudText>
                                }
                            </MudStack>
                        </MudPaper>
                    }

                    @if (_selectedTrainingSession.Warmup.Count > 0)
                    {
                        <MudPaper Elevation="0" Class="pa-3 pl-bottom-sheet-item">
                            <MudStack Spacing="1">
                                <MudText Typo="Typo.subtitle2">@L["Warmup"]</MudText>
                                @foreach (var activity in _selectedTrainingSession.Warmup)
                                {
                                    <MudStack Spacing="0">
                                        <MudText Typo="Typo.body2">@activity.Title</MudText>
                                        <MudText Typo="Typo.caption" Color="Color.Secondary">@GetActivityDetail(activity)</MudText>
                                    </MudStack>
                                }
                            </MudStack>
                        </MudPaper>
                    }

                    @if (_selectedTrainingSession.Drills.Count > 0)
                    {
                        <MudPaper Elevation="0" Class="pa-3 pl-bottom-sheet-item">
                            <MudStack Spacing="1">
                                <MudText Typo="Typo.subtitle2">@L["Drills"]</MudText>
                                @foreach (var activity in _selectedTrainingSession.Drills)
                                {
                                    <MudStack Spacing="0">
                                        <MudText Typo="Typo.body2">@activity.Title</MudText>
                                        <MudText Typo="Typo.caption" Color="Color.Secondary">@GetActivityDetail(activity)</MudText>
                                    </MudStack>
                                }
                            </MudStack>
                        </MudPaper>
                    }

                    @if (_selectedTrainingSession.Workouts.Count > 0)
                    {
                        @foreach (var workout in _selectedTrainingSession.Workouts)
                        {
                            <MudPaper Elevation="0" Class="pa-3 pl-bottom-sheet-item">
                                <MudStack Spacing="1">
                                    <MudText Typo="Typo.subtitle2">@(!string.IsNullOrWhiteSpace(workout.Name) ? workout.Name : workout.WorkoutId)</MudText>
                                    <MudText Typo="Typo.caption" Color="Color.Secondary">
                                        @($"{workout.Sets} Set(s) · {workout.Rounds} Round(s)")
                                    </MudText>
                                </MudStack>
                            </MudPaper>
                        }
                    }

                    @if (_resolved is not null)
                    {
                        @foreach (var rs in _resolved.Segments)
                        {
                            var paceZoneClass = GetPaceZoneClass(rs.Segment.PaceKey);
                            var paceZoneCode = GetPaceZoneCode(rs.Segment.PaceKey);

                            <MudPaper Elevation="0" Class="@($"pa-3 pl-bottom-sheet-item {paceZoneClass}")">
                                <MudStack Row="true" AlignItems="AlignItems.Center" Spacing="2">
                                    @if (!string.IsNullOrEmpty(paceZoneCode))
                                    {
                                        <div class="pl-pace-zone-mark pl-pace-zone-mark-small" aria-hidden="true">
                                            @paceZoneCode
                                        </div>
                                    }

                                    <MudStack Spacing="1" Style="min-width:0;">
                                        <MudText Typo="Typo.subtitle2">@rs.Segment.Type</MudText>

                                        <MudText Typo="Typo.caption" Color="Color.Secondary">
                                            @(rs.Segment.Distance > 0 ? $"{rs.Segment.Distance} m" : "-")
                                            @(" · ")
                                            @(rs.Pace is null ? "-" : $"{rs.Pace:mm\\:ss}/km")
                                        </MudText>

                                        @if (rs.SegmentTime is not null || rs.LapTime is not null)
                                        {
                                            <MudText Typo="Typo.caption" Color="Color.Secondary">
                                                @(rs.SegmentTime is null ? "" : $"Segment: {rs.SegmentTime:hh\\:mm\\:ss}".Trim())
                                                @(rs.SegmentTime is not null && rs.LapTime is not null ? " · " : "")
                                                @(rs.LapTime is null ? "" : $"Lap: {rs.LapTime:mm\\:ss}")
                                            </MudText>
                                        }
                                    </MudStack>
                                </MudStack>
                            </MudPaper>
                        }
                    }
                </MudStack>
            }
        </div>
    </MudPaper>
    </MudFocusTrap>
</div>

</MudContainer>

@code {
    [Parameter]
    public bool Embedded { get; set; }

    [Parameter]
    public bool AutoScrollToNextSession { get; set; } = true;

    private TrainingPlan? Plan;
    private string? _nextSessionId;
    private AthleteModel? _athlete;
    private string? _userId;

    private bool _detailsOpen;

    private async Task HandlePlanCardKeyDown(KeyboardEventArgs args, string planId)
    {
        if (args.Key is "Enter" or " ")
            await SelectPlanAsync(planId);
    }

    private void HandleDetailsKeyDown(KeyboardEventArgs args)
    {
        if (args.Key == "Escape")
            CloseDetails();
    }
    private ResolvedRunningSession? _resolved;
    private TrainingSession? _selectedTrainingSession;

    private PaceModel _paceModel = default!;

    private List<TrainingPlan> _plans = new();
    private string? _selectedPlanId;
    private HashSet<string> _subscribedPlanIds = new(StringComparer.OrdinalIgnoreCase);
    private HashSet<string> _completedSessionKeys = new(StringComparer.OrdinalIgnoreCase);
    private bool _isSavingCompletion;

    private string ContainerClass => Embedded
        ? "pa-0 pb-8"
        : "pt-6 px-6 pb-8";

    protected override async Task OnInitializedAsync()
    {
        if (!Embedded)
        {
            Navigation.NavigateTo("/Athletes/courses", replace: true);
            return;
        }

        try
        {
            // athlete laden
            var authState = await GetAuthenticationStateAsync.GetAuthenticationStateAsync();
            var userID = authState.User.FindFirst(u => u.Type.Contains("nameidentifier"))?.Value;

            if (!string.IsNullOrEmpty(userID))
            {
                _userId = userID;
                _athlete = await AthleteData.GetAthlete(userID);

                if (_athlete?.PaceModel is not null)
                    _paceModel = _athlete.PaceModel;
            }

            _plans = (await TrainingPlanService.LoadTrainingPlansForUserAsync(userID)).ToList();
            await LoadPlanSubscriptionsAsync(userID);
            await LoadTrainingSessionCompletionsAsync(userID);

            // Auswahl: gespeicherter Plan wenn vorhanden (Server-Athlete.SelectedTrainingPlanId), sonst Browser localStorage, sonst nächster oder erster Plan
            string? storedPlan = null;

            if (_athlete?.SelectedTrainingPlanId is not null && _plans.Any(p => p.Id == _athlete.SelectedTrainingPlanId))
            {
                _selectedPlanId = _athlete.SelectedTrainingPlanId;
            }
            else
            {
                if (!string.IsNullOrEmpty(userID))
                {
                    try
                    {
                        var key = $"selectedPlan_{userID}";
                        storedPlan = await JS.InvokeAsync<string>("localStorage.getItem", key);
                    }
                    catch
                    {
                        // ignore JS/localStorage errors
                    }
                }

                if (!string.IsNullOrEmpty(storedPlan) && _plans.Any(p => p.Id == storedPlan))
                {
                    _selectedPlanId = storedPlan;
                }
                else
                {
                    // nächster Plan finden
                    var today = DateTime.Today;
                    var next = _plans
                        .Where(p => p.Sessions.Any())
                        .SelectMany(p => p.Sessions.Select(s => (Plan: p, Session: s)))
                        .Where(x => x.Session.Date.Date >= today)
                        .OrderBy(x => x.Session.Date)
                        .FirstOrDefault();

                    if (next != default)
                        _selectedPlanId = next.Plan.Id;
                    else
                        _selectedPlanId = _plans.FirstOrDefault()?.Id;
                }
            }

            // Setze aktuell gewählten Plan
            ApplySelectedPlan();
        }
        catch
        {
            await dialogService.ShowInformationAsync(
                L["Error_Title"],
                L["Error_LoadData"],
                Icons.Material.Filled.Error);
        }
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender && AutoScrollToNextSession && _nextSessionId != null)
            await JS.InvokeVoidAsync("scrollToNextSession");
    }

    private void OpenDetails(string sessionId)
    {
        if (Plan is null) return;
        var session = Plan.Sessions.FirstOrDefault(s => s.Id == sessionId);
        if (session is null) return;

        _selectedTrainingSession = session;
        var run = session.PrimaryRun;
        _resolved = run is null ? null : RunningSessionResolver.Resolve(run, _paceModel);
        _detailsOpen = true;
    }

    private void CloseDetails()
    {
        _detailsOpen = false;
    }

    private async Task SelectPlanAsync(string planId)
    {
        _selectedPlanId = planId;
        ApplySelectedPlan();

        if (!string.IsNullOrEmpty(_userId))
        {
            try
            {
                var key = $"selectedPlan_{_userId}";
                await JS.InvokeVoidAsync("localStorage.setItem", key, _selectedPlanId);
            }
            catch
            {
                await dialogService.ShowInformationAsync(L["Error_SaveBrowser_Title"], L["Error_SaveBrowser"], Icons.Material.Filled.Error);
            }
        }

        if (_athlete is not null)
        {
            try
            {
                _athlete.SelectedTrainingPlanId = _selectedPlanId;
                await AthleteData.UpdateAthlete(_athlete);
            }
            catch
            {
                await dialogService.ShowInformationAsync(L["Error_SaveServer_Title"], L["Error_SaveServer"], Icons.Material.Filled.Error);
            }
        }

        StateHasChanged();
    }

    private void ApplySelectedPlan()
    {
        Plan = _plans.FirstOrDefault(p => p.Id == _selectedPlanId);

        var today = DateTime.Today;
        _nextSessionId = Plan?.Sessions
            .Where(s => s.Date.Date >= today)
            .OrderBy(s => s.Date)
            .Select(s => s.Id)
            .FirstOrDefault();
    }

    private bool IsPlanSubscribed(string planId)
    {
        return _subscribedPlanIds.Contains(planId);
    }

    private async Task TogglePlanSubscriptionAsync(string planId)
    {
        if (!_subscribedPlanIds.Add(planId))
            _subscribedPlanIds.Remove(planId);

        await SavePlanSubscriptionsAsync();
    }

    private async Task LoadPlanSubscriptionsAsync(string? userId)
    {
        _subscribedPlanIds.Clear();

        if (string.IsNullOrWhiteSpace(userId))
            return;

        try
        {
            var stored = await JS.InvokeAsync<string?>("localStorage.getItem", CalendarSubscriptionStorageKey(userId));
            if (string.IsNullOrWhiteSpace(stored))
                return;

            var availablePlanIds = _plans.Select(plan => plan.Id).ToHashSet(StringComparer.OrdinalIgnoreCase);
            _subscribedPlanIds = stored
                .Split('|', StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries)
                .Where(availablePlanIds.Contains)
                .ToHashSet(StringComparer.OrdinalIgnoreCase);
        }
        catch
        {
            _subscribedPlanIds.Clear();
        }
    }

    private async Task SavePlanSubscriptionsAsync()
    {
        if (string.IsNullOrWhiteSpace(_userId))
            return;

        try
        {
            var value = string.Join("|", _subscribedPlanIds.OrderBy(planId => planId));
            await JS.InvokeVoidAsync("localStorage.setItem", CalendarSubscriptionStorageKey(_userId), value);
        }
        catch
        {
            await dialogService.ShowInformationAsync(L["Error_SaveBrowser_Title"], L["Error_SaveBrowser"], Icons.Material.Filled.Error);
        }
    }

    private static string GetPaceZoneClass(string? paceKey)
    {
        return paceKey switch
        {
            PaceKeys.Free => "pl-pace-zone-free",
            PaceKeys.Recovery => "pl-pace-zone-recovery",
            PaceKeys.Easy => "pl-pace-zone-easy",
            PaceKeys.Marathon => "pl-pace-zone-marathon",
            PaceKeys.Threshold => "pl-pace-zone-threshold",
            PaceKeys.Intervall => "pl-pace-zone-intervall",
            PaceKeys.Repetition => "pl-pace-zone-repetition",
            _ => string.Empty
        };
    }

    private static string GetPaceZoneCode(string? paceKey)
    {
        return paceKey switch
        {
            PaceKeys.Free => "Free",
            PaceKeys.Recovery => "Rec",
            PaceKeys.Easy => "E",
            PaceKeys.Marathon => "M",
            PaceKeys.Threshold => "T",
            PaceKeys.Intervall => "I",
            PaceKeys.Repetition => "R",
            _ => string.Empty
        };
    }

    private string GetSessionTimelineDetail(TrainingSession session)
    {
        var parts = new List<string> { FormatSessionSchedule(session) };
        AddSessionComposition(parts, session, null);
        return string.Join(" · ", parts);
    }

    private string GetSessionDetailSummary(TrainingSession session)
    {
        var parts = new List<string> { FormatSessionSchedule(session) };
        AddSessionComposition(parts, session, _resolved);
        return string.Join(" · ", parts);
    }

    private void AddSessionComposition(
        List<string> parts,
        TrainingSession session,
        ResolvedRunningSession? resolved)
    {
        if (resolved is not null)
            parts.Add($"{resolved.TotalDistance / 1000.0:0.0} km");
        else if (session.PrimaryRun is not null)
            parts.Add($"{session.PrimaryRun.TotalDistance / 1000.0:0.0} km");

        if (session.Workouts.Count > 0)
            parts.Add(string.Format(L["WorkoutCount"], session.Workouts.Count));

        if (session.Warmup.Count > 0)
            parts.Add(string.Format(L["WarmupCount"], session.Warmup.Count));

        if (session.Drills.Count > 0)
            parts.Add(string.Format(L["DrillCount"], session.Drills.Count));
    }

    private string FormatSessionSchedule(TrainingSession session)
    {
        return session.Appointment.StartsAt is null
            ? session.Date.ToString("dd.MM.yyyy")
            : FormatAppointment(session.Appointment, session.Date);
    }

    private static string FormatAppointment(TrainingSessionAppointment appointment, DateTime? fallbackDate = null)
    {
        if (appointment.StartsAt is null)
            return fallbackDate?.ToString("dd.MM.yyyy") ?? string.Empty;

        if (appointment.EndsAt is null)
            return appointment.StartsAt.Value.ToString("dd.MM.yyyy HH:mm");

        return appointment.StartsAt.Value.Date == appointment.EndsAt.Value.Date
            ? $"{appointment.StartsAt.Value:dd.MM.yyyy HH:mm} - {appointment.EndsAt.Value:HH:mm}"
            : $"{appointment.StartsAt.Value:g} - {appointment.EndsAt.Value:g}";
    }

    private IEnumerable<string> GetTrainingEffectLines(TrainingEffect effect)
    {
        if (!string.IsNullOrWhiteSpace(effect.Focus))
            yield return $"{L["Focus"]}: {effect.Focus}";

        if (!string.IsNullOrWhiteSpace(effect.Stimulus))
            yield return $"{L["Stimulus"]}: {effect.Stimulus}";

        if (!string.IsNullOrWhiteSpace(effect.Adaptation))
            yield return $"{L["Adaptation"]}: {effect.Adaptation}";

        if (!string.IsNullOrWhiteSpace(effect.Recovery))
            yield return $"{L["Recovery"]}: {effect.Recovery}";
    }

    private string GetActivityDetail(TrainingSessionActivity activity)
    {
        var parts = new List<string>();

        if (activity.DurationSeconds > 0)
            parts.Add(FormatDuration(activity.DurationSeconds));

        if (!string.IsNullOrWhiteSpace(activity.ActivityType))
            parts.Add(activity.ActivityType);

        if (!string.IsNullOrWhiteSpace(activity.ReferenceId))
            parts.Add(activity.ReferenceId);

        if (!string.IsNullOrWhiteSpace(activity.Description))
            parts.Add(activity.Description);

        return parts.Count == 0 ? "-" : string.Join(" · ", parts);
    }

    private static string FormatDuration(int seconds)
    {
        if (seconds < 60)
            return $"{seconds} s";

        var minutes = seconds / 60;
        var remainingSeconds = seconds % 60;
        return remainingSeconds == 0
            ? $"{minutes} min"
            : $"{minutes}:{remainingSeconds:00} min";
    }

    private static string GetPlanCardClass(bool isSelected)
    {
        return isSelected
            ? "pa-3 pl-training-plan-card pl-training-plan-card-selected"
            : "pa-3 pl-training-plan-card";
    }

    private static string GetSessionRowClass(bool isNext)
    {
        return isNext
            ? "pl-training-session-row pl-training-session-row-next"
            : "pl-training-session-row";
    }

    private static string GetSessionRowShellClass(bool isNext)
    {
        return isNext
            ? "pl-training-session-row-shell pl-training-session-row-shell-next"
            : "pl-training-session-row-shell";
    }

    private bool IsSessionCompleted(TrainingSession session)
    {
        return Plan is not null && _completedSessionKeys.Contains(CompletionKey(Plan.Id, session.Id));
    }

    private string GetCompletionTooltip(TrainingSession session)
    {
        return IsSessionCompleted(session)
            ? "Einheit als offen markieren"
            : "Einheit als absolviert abhaken";
    }

    private async Task ToggleSessionCompletionAsync(TrainingSession session, bool isCompleted)
    {
        if (Plan is null || string.IsNullOrWhiteSpace(_userId) || _isSavingCompletion)
            return;

        var key = CompletionKey(Plan.Id, session.Id);
        _isSavingCompletion = true;

        try
        {
            var result = await AchievementService.SetTrainingSessionCompletionAsync(
                _userId,
                Plan.Id,
                session.Id,
                isCompleted);

            if (isCompleted)
                _completedSessionKeys.Add(key);
            else
                _completedSessionKeys.Remove(key);

            foreach (var award in result.Awarded)
                Snackbar.Add($"Achievement freigeschaltet: {award.TitleSnapshot}", Severity.Success);
        }
        catch
        {
            Snackbar.Add("Der Status der Einheit konnte nicht gespeichert werden.", Severity.Error);
        }
        finally
        {
            _isSavingCompletion = false;
        }
    }

    private async Task LoadTrainingSessionCompletionsAsync(string? userId)
    {
        _completedSessionKeys.Clear();

        if (string.IsNullOrWhiteSpace(userId))
            return;

        try
        {
            var completions = await AchievementService.GetTrainingSessionCompletionsForAthleteAsync(userId);
            _completedSessionKeys = completions
                .Select(completion => CompletionKey(completion.PlanId, completion.SessionId))
                .ToHashSet(StringComparer.OrdinalIgnoreCase);
        }
        catch
        {
            _completedSessionKeys.Clear();
        }
    }

    private static string CompletionKey(string planId, string sessionId)
    {
        return $"{planId}:{sessionId}";
    }

    private string FormatPlanSummary(TrainingPlan plan)
    {
        return string.Format(L["PlanSessionCount"], plan.Sessions.Count);
    }

    private string FormatPlanRange(TrainingPlan plan)
    {
        return plan.StartDate is null || plan.EndDate is null
            ? L["PlanNoDates"]
            : string.Format(L["PlanRange"], plan.StartDate.Value, plan.EndDate.Value);
    }

    private static string CalendarSubscriptionStorageKey(string userId)
    {
        return $"trainingCalendarSubscribedPlans_{userId}";
    }
}
