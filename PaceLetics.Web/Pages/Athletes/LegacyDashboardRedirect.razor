@page "/Athletes/dashboard"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        var current = new Uri(Navigation.Uri);
        Navigation.NavigateTo($"/{current.Query}", replace: true);
    }
}
