@page "/Athletes/racepaces"
@page "/Athletes/pacezones"

@using AthleteDataAccessLibrary;
@using AthleteDataAccessLibrary.Contracts;
@using MudBlazor;
@using PaceLetics.AthleteModule.CodeBase.Interfaces
@using PaceLetics.CoreModule.Infrastructure.Interfaces
@using PaceLetics.AthleteModule.Components

@inject AuthenticationStateProvider GetAuthenticationStateAsync
@inject IAthleteData AthleteData
@inject IDialogService dialogService
@inject ICriticalSpeedService criticalSpeedService
@inject LoadingStateService Loading
@inject IStringLocalizer<RacePaces> L
@inject IJSRuntime JS

@if (!Embedded)
{
    <PageTitle>@L["PageTitle"]</PageTitle>
}

<MudContainer Class="@ContainerClass" MaxWidth="MaxWidth.Small">

    @if (!Embedded)
    {
        <PageHeader Title="@L["Title"]"
                    Subtitle="@L["Subtitle"]" />

        <SectionNav Items="@SectionItems" ActiveKey="@(_activeSection == RacePacesSection.PaceZones ? "pacezones" : "racedata")" OnSelect="SelectSectionByKey" AriaLabel="@L["Title"]" />

        <MudDivider Class="my-4" />
    }

    <MudStack Spacing="3">

        @if (_activeSection == RacePacesSection.RaceData)
        {
            @if (ReferenceRuns.Count == 0)
            {
                <ActionableEmptyState Title="@L["NoRaceTitle"]"
                                      Detail="@L["NoRaceWarning"]"
                                      ActionText="@L["AddRace"]"
                                      OnAction="AddReferenceRun" />
            }
            else
            {
                @foreach (var raceResult in ReferenceRuns)
                {
                    <RaceCard @key="raceResult"
                              OnEditRaceCard="EditReferenceRun"
                              OnDeleteRaceCard="DeleteReferenceRun"
                              Model="raceResult" />
                }

                <MudTooltip Text="@L["AddRace"]">
                    <MudIconButton Variant="Variant.Outlined"
                                   Color="Color.Primary"
                                   Icon="@Icons.Material.Filled.AddCircle"
                                   OnClick="AddReferenceRun"
                                   aria-label="@L["AddRace"]" />
                </MudTooltip>

                @if (_athlete.ActiveReferenceResult is not null
                    && (DateTime.Now - _athlete.ActiveReferenceResult.Date).TotalDays > 180)
                {
                    <MudAlert Severity="Severity.Info" Variant="Variant.Outlined">
                        @L["RaceOldWarning"]
                    </MudAlert>
                }

                @if (_criticalSpeed.IsValid)
                {
                    <MudPaper Class="pa-4 pl-critical-speed-card" Elevation="0">
                        <MudStack Spacing="1">
                            <MudText Typo="Typo.subtitle1" Class="fw-bold">@L["CriticalSpeedCard_Title"]</MudText>
                            <MudText Typo="Typo.h5">@($"{_criticalSpeed.CriticalSpeedMps:0.00} m/s")</MudText>
                            <MudText Typo="Typo.body2" Color="Color.Secondary">@GetCriticalSpeedMethodText()</MudText>
                        </MudStack>
                    </MudPaper>
                }
            }
        }
        else if (_activeSection == RacePacesSection.PaceZones)
        {
            <MudStack Spacing="1">
                <MudText Typo="Typo.h6">@L["PaceZonesTitle"]</MudText>
                <MudText Typo="Typo.body2" Color="Color.Secondary">@L["PaceZonesSubtitle"]</MudText>
            </MudStack>

            @if (!_criticalSpeed.IsValid)
            {
                <MudAlert Severity="Severity.Warning" Variant="Variant.Outlined">
                    @L["NoRaceWarning"]
                </MudAlert>
            }

            else
            {
                <CriticalSpeedBudgetInfo Model="@_criticalSpeed"
                                         Recommendations="@_criticalSpeedIntervals" />
            }
        }
    </MudStack>

</MudContainer>

@code {
    private enum RacePacesSection
    {
        RaceData,
        PaceZones
    }

    private RacePacesSection _activeSection = RacePacesSection.RaceData;
    private IReadOnlyList<SectionNavEntry> SectionItems =>
    [
        new("racedata", L["TabRaceData"]),
        new("pacezones", L["TabPaceZones"])
    ];

    private void SelectSection(RacePacesSection section)
    {
        _activeSection = section;
    }

    private void SelectSectionByKey(string key) => SelectSection(
        string.Equals(key, "pacezones", StringComparison.OrdinalIgnoreCase)
            ? RacePacesSection.PaceZones
            : RacePacesSection.RaceData);

    private string GetCriticalSpeedMethodText()
    {
        return _criticalSpeed.Method switch
        {
            PaceLetics.CoreModule.Infrastructure.Enums.CriticalSpeedMethod.Single1200Estimate => L["CriticalSpeedCard_Method_1200"],
            PaceLetics.CoreModule.Infrastructure.Enums.CriticalSpeedMethod.TwoPoint1200And3600 => L["CriticalSpeedCard_Method_1200_3600"],
            PaceLetics.CoreModule.Infrastructure.Enums.CriticalSpeedMethod.MultiPointRegression => L["CriticalSpeedCard_Method_Regression"],
            PaceLetics.CoreModule.Infrastructure.Enums.CriticalSpeedMethod.SinglePerformanceEstimate => L["CriticalSpeedCard_Method_Single"],
            _ => L["CriticalSpeedCard_Method_None"]
        };
    }
}
